[build]
command = "npm install --frozen-lockfile"

[deploy]
startCommand = "node autoresearch-fpl/run.mjs backtest"

[env]
NODE_ENV = "production"

[deployments.production]
command = "node autoresearch-fpl/run.mjs backtest"

[[services]]
name = "fpl-autoresearch"
port = 3000
healthcheck = "/health"